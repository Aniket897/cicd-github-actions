name: first-cicd-pipeline
on: [push]
jobs:
  install-node-and-check:
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: "14"
      - run: node -v
